# macOS PyInstaller（预留）

如果后续决定在 macOS 上也将后端/worker 打成独立可执行文件（类似 Windows 的 `backend_server.exe` / `quality_worker.exe`），建议将对应的 `.spec` 放在这里：

- `packaging/macos/pyinstaller/`

目前仓库仅维护 Windows 交付链路（对应位置：`packaging/windows/pyinstaller/`）。

