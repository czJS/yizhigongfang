"""
Pipelines entry scripts (repo layout v2).

Stage A: keep logic in `scripts/` and delegate from these thin wrappers.
Stage B: logic will be refactored into `pipelines/lib/` and these will become the true
entrypoints.
"""

