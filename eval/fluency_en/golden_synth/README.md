# golden_synth（合成金标，仅用于内部评测）

这里的 “golden” 不是人工审校金标，而是：
**基于公开集的 ref_en 作为语义参考 → 用本地 LLM 改写成更口语、更像字幕的英文**。

用途：
- 用于你们“允许改写、更母语”的方向做快速回归对比
- 适合内部实验与迭代 prompt/增强开关

不建议：
- 作为对外宣传的“绝对金标”
- 公开发布（它是公开数据的衍生物 + 模型生成）

生成命令见 `eval/TESTING_GUIDE.md`。





