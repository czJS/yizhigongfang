# apps/backend

Repo layout v2 placeholder.

- **Current implementation (transition)**: backend code still lives under `backend/`.
- **Packaging entry**: `backend_server.exe` is built from `backend/app.py` via `packaging/pyinstaller/backend_server.spec`.

Stage C will migrate code into `apps/backend/` and remove legacy paths.

