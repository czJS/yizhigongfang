# 云资源连接信息记录（脱敏）

> 目的：记录当前一期登录与授权环境的云资源连接信息。  
> 重要提醒：**不要把明文密码提交到 Git 仓库或写入 `docs/`**。密码请单独保存在密码管理器、系统钥匙串，或放到不入库的本地 `.env` 文件中。

---

## 1. ECS

- **公网 IP**：`8.149.245.13`
- **内网 IP**：`172.30.12.139`
- **系统**：`Ubuntu 24.04 64位`
- **用途**：`Nginx + 登录/授权后端 + 管理后台`
- **已部署服务目录**：`/srv/ygf/auth-service`
- **systemd 服务名**：`ygf-auth.service`
- **本机监听端口**：`8001`
- **公网临时访问路径**：`http://8.149.245.13/auth-api/`
- **健康检查**：`http://8.149.245.13/auth-api/api/health`

---

## 2. RDS MySQL

- **RDS 内网地址**：`rm-bp1e5cdl4qvu7799z.mysql.rds.aliyuncs.com`
- **端口**：`3306`
- **数据库名**：`mych_prod`
- **数据库账号**：`mych_app`
- **数据库密码**：`请保存在密码管理器，不要写入仓库`

### 2.1 连接命令

```bash
mysql -h rm-bp1e5cdl4qvu7799z.mysql.rds.aliyuncs.com -P 3306 -u mych_app -p
```

### 2.2 连接验证

连接成功后可执行：

```sql
SHOW DATABASES;
USE mych_prod;
SELECT DATABASE();
```

---

## 3. RDS 白名单

- **允许访问的 ECS 私网 IP**：`172.30.12.139`
- **白名单分组**：`ecs_app`

---

## 4. 安全组开放端口

- `22/22`：SSH
- `80/80`：HTTP
- `443/443`：HTTPS

---

## 5. 后续应用环境变量建议

后端部署时建议使用本地环境变量文件，不要把密码写进代码仓库。

```env
DB_HOST=rm-bp1e5cdl4qvu7799z.mysql.rds.aliyuncs.com
DB_PORT=3306
DB_NAME=mych_prod
DB_USER=mych_app
DB_PASSWORD=请单独填写到本地.env，不要提交
```

### 5.1 认证服务部署补充

- **远端环境文件**：`/srv/ygf/auth-service/.env.local`
- **当前联调模式**：`AUTH_DEV_ECHO_CODES=1`
- **说明**：在 DirectMail / 域名解析未完全就绪前，验证码接口会回显 `dev_code` 用于联调。

---

## 6. Redis / Tair

### 6.1 当前已确认的实例信息

- **产品类型**：`Redis 开源版`
- **实例 ID**：`r-bp1q5dbmkvdvok2xj6`
- **地域 / 可用区**：`华东1（杭州）/ 杭州J`
- **专有网络 VPC**：`vpc-bp15b17vrml4mzhagx95p`
- **交换机**：`vsw-bp1qfrofthtu0jgrdlb8p4`
- **版本**：`7.0`
- **规格**：`1 GB`
- **部署模式**：`云原生`
- **状态**：`运行中`
- **到期时间**：`2027-04-02 00:00:00`
- **最大连接数**：`10,000`
- **带宽 MB/s**：`48 MB`
- **自动续费**：`当前关闭`

### 6.2 待补充的连接信息

以下信息在当前截图中未完整展示，后续请在控制台连接信息页补齐：

- **Redis 内网地址**：`r-bp1q5dbmkvdvok2xj6.redis.rds.aliyuncs.com`
- **端口**：`6379`
- **Redis 密码**：`请保存在密码管理器，不要写入仓库`
- **白名单分组**：`待补充`

### 6.3 Redis 连接命令模板

```bash
redis-cli -h r-bp1q5dbmkvdvok2xj6.redis.rds.aliyuncs.com -p 6379 -a '<REDIS_PASSWORD>' ping
```

如果返回：

```text
PONG
```

说明 Redis 已连通。

---

## 7. Redis / Tair 控制台说明

阿里云控制台里，Redis 产品通常显示为：

- `云数据库 Tair（兼容 Redis）`

也就是说，如果你在控制台里没搜到“Redis”，请改搜下面这些关键词：

- `Tair`
- `云数据库 Tair`
- `兼容 Redis`

## 7.1 一期购买建议

一期只做邮箱验证码登录、会话和限流时，优先选择：

- 同地域：`华东1（杭州）`
- 同 VPC
- 基础可用的小规格实例

买完后和 RDS 一样：

1. 找到实例内网地址
2. 配置白名单，加入 ECS 私网 IP `172.30.12.139`
3. 记录 Redis 连接信息到本地 `.env`

---

## 8. 后续应用环境变量建议（补充 Redis）

```env
REDIS_HOST=r-bp1q5dbmkvdvok2xj6.redis.rds.aliyuncs.com
REDIS_PORT=6379
REDIS_PASSWORD=请单独填写到本地.env，不要提交
```

---

## 9. 密码管理建议

当前这套环境至少有以下密码/密钥需要分开保存：

- ECS root 密码
- RDS 数据库账号密码
- Redis 密码
- 邮件推送 SMTP 密码
- 应用 JWT / Session Secret

建议保存位置：

- 系统钥匙串
- 1Password / Bitwarden / iCloud Passwords
- 本地 `.env` 文件（并确保加入 `.gitignore`）

不要保存到：

- `docs/`
- `README`
- 代码文件
- 聊天记录截图

