# macOS 签名与公证（可选，预留）

> 说明：若你们要对外分发（非内测），通常需要 Developer ID 签名与 Notarization（公证）。
> 这里先预留文档入口，后续按你们的 Apple 开发者账号与交付方式补齐。

## 建议包含的内容（后续补齐）

- 证书与身份（Developer ID Application / Installer）
- `electron-builder` 的 mac 配置（签名、entitlements、hardened runtime）
- 公证流程（notarytool / altool）
- 常见错误与排查（Gatekeeper、权限、证书链、缓存）

