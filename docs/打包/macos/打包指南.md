# macOS 打包指南（预留）

> 说明：当前仓库已经有完整的 Windows 打包流程；macOS 这里先放“文档入口”和待办项，等你们决定正式支持后再补齐脚本与流程。

## 0. 交付物（建议对齐 Windows）

- 安装包（建议使用 `electron-builder` 的 dmg/zip 或 pkg）
- 模型包 `models_pack.zip`（建议与 Windows 复用同一套“模型文件清单/组包规则”）
- （如质量模式依赖本地 LLM）Ollama 包（是否需要单独交付，取决于你们的 macOS 目标形态）

## 1. 打包入口（脚本位置）

- 预留：`packaging/macos/scripts/`

## 2. 签名与公证

详见：`docs/打包/macos/签名与公证.md`

