# macOS 打包机准备清单（预留）

> 说明：macOS 打包是否要做，取决于你们的交付目标（内测/外发/上架）。
> 这里先预留入口，等确定后再补齐“必须的软件、目录约定、缓存位置、签名工具链”等内容。

## 建议准备项（后续补齐）

- Xcode Command Line Tools
- Node.js（与 Windows 对齐版本）
- Python（如仍需 PyInstaller 产出独立后端/worker，则需要）
- Apple Developer 账号与证书（如需签名/公证）

